void setup() {
  // put your setup code here, to run once:
  serial.begin(9600)
  String word = [fate]
  Serial.println(fate); // writes the word
  Serial.println(fate [0]);//writes the first letter
  fate
  String.juanito = fate [1]// second letter
  Serial.println(juanito)
  fate[1]= fate [0];
  fate[0]= juanito
  Serial.println(fate);// actually, it just chage ther order of the letter, for an ex: fate becomes to afte

  //String functionX(String W){
  // String T = W
  //W[1]= W[0];
  // W[0] = T;
  // return W;
  //}
}


}

void loop() {
  // put your main code here, to run repeatedly:
